spring:
  application:
    name: booking-service
  h2:
    console.enabled: true
  datasource:
    url: jdbc:h2:mem:booking-service-db
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    defer-datasource-initialization: true
